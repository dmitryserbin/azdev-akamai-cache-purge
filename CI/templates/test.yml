parameters:
  endpoint: ""
  network: ""
  purgeType: ""
  purgeMethod: "s"
  urls: ""

steps:
- task: akamaicachepurge@1
  displayName: Akamai Cache Purge
  inputs:
    edgegridEndpoint: ${{ parameters.endpoint }}
    network: ${{ parameters.network }}
    purgeType: ${{ parameters.purgetype }}
    urls: ${{ parameters.urls }}
    purgeMethod: ${{ parameters.purgemethod }}
